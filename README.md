# node-restserver-test
Servidor REST en Node

Un rest server para autenticacipón de usuarios hecho nodejs usando express, jsonwebtoken y una base de datos mongodb.

Iniciar con los siguinentes comandos:

```bash
npm install

npm start
```
